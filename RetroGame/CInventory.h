#pragma once 
#include "pch.h"
#include <vector>
#include "CObject.h"

using namespace std;
using namespace sf;

const unsigned int INVENTORY_SPACE = 24;

class CInventory
{
	unsigned int _Currency;
	vector<CObject*> _Objects;
public:

	////////////////////////////////////////////////////////////
	// Constructeur
	////////////////////////////////////////////////////////////
	CInventory();

	////////////////////////////////////////////////////////////
	// Get amount of (money/gold/currency)
	////////////////////////////////////////////////////////////
	int GetCurrency() const;

	////////////////////////////////////////////////////////////
	// Get amount of (money/gold/currency)
	////////////////////////////////////////////////////////////
	void SetCurrency(int amount);

	////////////////////////////////////////////////////////////
	// Get number of objects in the vector
	////////////////////////////////////////////////////////////
	int GetNbObjects() const;

	////////////////////////////////////////////////////////////
	// Get number of empty place in inventory
	////////////////////////////////////////////////////////////
	int GetNbEmpty() const;

	////////////////////////////////////////////////////////////
	// Return an object with is index
	////////////////////////////////////////////////////////////
	CObject* GetObject(int index);

	////////////////////////////////////////////////////////////
	// Add an object in the inventory
	////////////////////////////////////////////////////////////
	bool AddObject(CObject* object);

	////////////////////////////////////////////////////////////
	// Remove an object from the inventory
	////////////////////////////////////////////////////////////
	bool RemoveObject(CObject* object);

};