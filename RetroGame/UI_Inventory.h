#pragma once
#include "CPlayer.h"
#include <iostream>
#include <string>

using namespace std;
using namespace sf;

const unsigned int NB_BAR_TO_FILL = 26;
const int WIDHT = 103;
const int HEIGHT = 200;

class UI_Inventory : public sf::Sprite
{
	// Attributs
	bool _Visible;
	CPlayer* _Player;

	Texture _TextureBackground;
	Texture _TextureLifeBar;
	Texture _TextureMagicBar;
	Font _Font;
	Text _TextStance;
	Text _TextAttack;
	Text _TextDefense;



	////////////////////////////////////////////////////////////
	// Display Objects in inventory
	////////////////////////////////////////////////////////////
	void DisplayObjects(RenderWindow& window);

public:

	////////////////////////////////////////////////////////////
	// Constructeur
	////////////////////////////////////////////////////////////
	UI_Inventory(CPlayer* player, Font& font);

	////////////////////////////////////////////////////////////
	// GetVisible
	////////////////////////////////////////////////////////////
	bool GetVisible() const;

	////////////////////////////////////////////////////////////
	// SetVisible
	////////////////////////////////////////////////////////////
	void SetVisible(bool visible);

	////////////////////////////////////////////////////////////
	// LoadTexture
	////////////////////////////////////////////////////////////
	void LoadTexture();

	////////////////////////////////////////////////////////////
	// InitTexts
	////////////////////////////////////////////////////////////
	void InitTexts();

	////////////////////////////////////////////////////////////
	// Display UI
	////////////////////////////////////////////////////////////
	void Display(RenderWindow& window);
	void DisplayTexts(RenderWindow& window);
	void DisplayLifeBar(RenderWindow& window);
	void DisplayMagicBar(RenderWindow& window);

	////////////////////////////////////////////////////////////
	// RefreshPosition
	////////////////////////////////////////////////////////////
	void RefreshPosition(RenderWindow& window, CMap& wowrld);

	////////////////////////////////////////////////////////////
	// Afficher
	////////////////////////////////////////////////////////////
	bool IsHoverObject(Vector2f pos);
};

