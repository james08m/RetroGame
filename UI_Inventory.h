#pragma once
#include "CPlayer.h"
#include <iostream>
#include <string>

using namespace std;
using namespace sf;

const int WIDHT = 103;
const int HEIGHT = 200;

class UI_Inventory : public sf::Sprite
{
	// Attributs
	bool _Visible;
	Texture _Texture;
	CPlayer* _Player;
	
	////////////////////////////////////////////////////////////
	// Display Objects in inventory
	////////////////////////////////////////////////////////////
	void DisplayObjects(RenderWindow& window);

public:

	////////////////////////////////////////////////////////////
	// Constructeur
	////////////////////////////////////////////////////////////
	UI_Inventory(CPosition pos, CPlayer* player);

	////////////////////////////////////////////////////////////
	// GetVisible
	////////////////////////////////////////////////////////////
	bool GetVisible() const;

	////////////////////////////////////////////////////////////
	// SetVisible
	////////////////////////////////////////////////////////////
	void SetVisible(bool visible);

	////////////////////////////////////////////////////////////
	// LoadTexture
	////////////////////////////////////////////////////////////
	void LoadTexture();

	////////////////////////////////////////////////////////////
	// Display UI
	////////////////////////////////////////////////////////////
	void Display(RenderWindow& window);

	////////////////////////////////////////////////////////////
	// RefreshPosition
	////////////////////////////////////////////////////////////
	void RefreshPosition(RenderWindow& window, CMap& wowrld);

	////////////////////////////////////////////////////////////
	// Afficher
	////////////////////////////////////////////////////////////
	bool IsHoverObject(Vector2f pos);
};
